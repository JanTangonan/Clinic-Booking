export default function LoginPage() {
  return (
    <div className="p-8 max-w-sm mx-auto">
      <h1 className="text-2xl font-semibold">Log in</h1>
      {/* TODO: wire up supabase.auth.signInWithPassword() via a client-side form */}
    </div>
  );
}
